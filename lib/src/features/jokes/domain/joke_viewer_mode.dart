/// Enum representing different modes for displaying jokes in the viewer.
enum JokeViewerMode {
  /// Reveal mode: Shows setup image first, then reveals punchline image
  reveal,

  /// Both adaptive mode: Shows both images simultaneously with adaptive layout
  bothAdaptive,
}
